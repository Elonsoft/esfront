import { Meta } from '@storybook/blocks';
import LinkTo from '@storybook/addon-links/react';
import { TableInterface } from '~storybook/components/TableInterface';

<Meta title="Components API/GallerySwiperImage" />

# GallerySwiperImage API

```js
import { GallerySwiperImage } from '@esfront/react';
```

## Component name

The name `ESGallerySwiperImage` can be used when providing default props or style overrides in the theme.

## Props

<TableInterface name="GallerySwiperImageProps" variant="props" />

## CSS

<TableInterface name="GallerySwiperImageClasses" variant="css" />

## Demos

<ul>
  <li>
    <LinkTo kind="components-gallery" story="demo">
      <code>Gallery</code>
    </LinkTo>
  </li>
</ul>
