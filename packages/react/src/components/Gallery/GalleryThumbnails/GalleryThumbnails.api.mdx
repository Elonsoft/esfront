import { Meta } from '@storybook/blocks';
import LinkTo from '@storybook/addon-links/react';
import { TableInterface } from '~storybook/components/TableInterface';

<Meta title="Components API/GalleryThumbnails" />

# GalleryThumbnails API

```js
import { GalleryThumbnails } from '@esfront/react';
```

## Component name

The name `ESGalleryThumbnails` can be used when providing default props or style overrides in the theme.

## Props

<TableInterface name="GalleryThumbnailsProps" variant="props" />

## CSS

<TableInterface name="GalleryThumbnailsClasses" variant="css" />

## Demos

<ul>
  <li>
    <LinkTo kind="components-gallery" story="demo">
      <code>Gallery</code>
    </LinkTo>
  </li>
</ul>
