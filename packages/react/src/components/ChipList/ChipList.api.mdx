import { Meta } from '@storybook/addon-docs';
import LinkTo from '@storybook/addon-links/react';
import { TableInterface } from '~storybook/components/TableInterface';

<Meta title="Components API/ChipList" />

# ChipList API

```js
import { ChipList } from '@elonkit/react';
```

## Component name

The name `ESChipList` can be used when providing default props or style overrides in the theme.

## Props

<TableInterface name="ChipListProps" variant="props" />

## CSS

<TableInterface name="ChipListClasses" variant="css" />

## Demos

<ul>
  <li>
    <LinkTo kind="components-ChipList" story="demo">
      <code>ChipList</code>
    </LinkTo>
  </li>
</ul>
