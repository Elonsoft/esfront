import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import LinkTo from '@storybook/addon-links/react';
import { Description } from '~storybook/components/Description';

import * as stories from './useResizeObserver.stories';

<Meta
  title="Hooks/useResizeObserver"
  parameters={{
    docs: {
      source: {
        type: 'code'
      }
    }
  }}
/>

# useResizeObserver

<Description name="useResizeObserver" />

<Canvas>
  <Story story={stories.Demo} name="Demo" />
</Canvas>

## API

<ul>
  <li>
    <LinkTo kind="hooks-api-useResizeObserver" story="page">
      <code>useResizeObserver</code>
    </LinkTo>
  </li>
</ul>
