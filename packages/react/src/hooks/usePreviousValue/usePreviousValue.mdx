import { Meta, Story, Canvas, Controls } from '@storybook/blocks';
import LinkTo from '@storybook/addon-links/react';
import { Description } from '~storybook/components/Description';

<Meta
  title="Hooks/usePreviousValue"
  parameters={{
    docs: {
      source: {
        type: 'code'
      }
    }
  }}
/>

# usePreviousValue

<Description name="usePreviousValue" />

## API

<ul>
  <li>
    <LinkTo kind="hooks-api-usePreviousValue" story="page">
      <code>usePreviousValue</code>
    </LinkTo>
  </li>
</ul>
