import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import LinkTo from '@storybook/addon-links/react';
import { Description } from '~storybook/components/Description';

import * as stories from './useIntersectionObserver.stories';

<Meta
  title="Hooks/useIntersectionObserver"
  parameters={{
    docs: {
      source: {
        type: 'code'
      }
    }
  }}
/>

# useIntersectionObserver

<Description name="useIntersectionObserver" />

<Canvas>
  <Story story={stories.Demo} name="Demo" />
</Canvas>

## API

<ul>
  <li>
    <LinkTo kind="hooks-api-useIntersectionObserver" story="page">
      <code>useIntersectionObserver</code>
    </LinkTo>
  </li>
</ul>
