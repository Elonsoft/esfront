import { Meta, Story, Canvas, Controls } from '@storybook/blocks';
import LinkTo from '@storybook/addon-links/react';
import { Description } from '~storybook/components/Description';

<Meta
  title="Hooks/useStuckSentinel"
  parameters={{
    docs: {
      source: {
        type: 'code'
      }
    }
  }}
/>

# useStuckSentinel

<Description name="useStuckSentinel" />

## API

<ul>
  <li>
    <LinkTo kind="hooks-api-useStuckSentinel" story="page">
      <code>useStuckSentinel</code>
    </LinkTo>
  </li>
</ul>
