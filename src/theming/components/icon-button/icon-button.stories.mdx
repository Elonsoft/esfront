import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import IconButton from '@mui/material/IconButton';
import Icon from '@mui/material/Icon';

<Meta
  title="Overrides/IconButton"
  parameters={{
    viewMode: 'canvas',
    options: { showPanel: true }
  }}
  argTypes={{
    color: {
      options: ['default', 'primary', 'secondary', 'error'],
      control: { type: 'select' }
    },
    size: {
      options: ['xs', 'sm', 'md', 'lg', 'xl'],
      control: { type: 'select' }
    },
    disabled: {
      control: { type: 'boolean' }
    }
  }}
/>

<Canvas>
  <Story name="IconButton">
    {(args, context) => {
      return (
        <IconButton {...args}>
          <Icon fontSize={args.size === 'xs' ? 'small' : 'medium'}>menu</Icon>
        </IconButton>
      );
    }}
  </Story>
</Canvas>
