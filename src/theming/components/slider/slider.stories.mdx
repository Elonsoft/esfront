import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import * as stories from './slider.stories';

<Meta
  title="Overrides/Slider"
  parameters={{
    viewMode: 'canvas'
  }}
  argTypes={{
    color: {
      options: ['primary', 'secondary'],
      control: { type: 'select' }
    },
    valueLabelDisplay: {
      options: ['off', 'on', 'auto'],
      control: { type: 'select' },
      defaultValue: 'auto'
    },
    disabled: {
      control: { type: 'boolean' }
    },
    height: {
      control: { type: 'range', min: 2, max: 8, step: 1 }
    }
  }}
/>

<Canvas>
  <Story name="Slider" story={stories.Demo} />
</Canvas>
