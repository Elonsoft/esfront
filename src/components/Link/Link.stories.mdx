import { Meta, Story, Canvas, ArgsTable, Props, Description } from '@storybook/addon-docs';
import LinkTo from '@storybook/addon-links/react';

import Typography from '@mui/material/Typography';

import { Link } from '.';

import { IconBookmark } from '../../icons';

<Meta
  title="Components/Link"
  component={Link}
  parameters={{
    options: { showPanel: true }
  }}
  argTypes={{
    component: {
      table: {
        disable: true
      }
    },
    children: {
      table: {
        disable: true
      }
    },
    classes: {
      table: {
        disable: true
      }
    },
    className: {
      table: {
        disable: true
      }
    },
    sx: {
      table: {
        disable: true
      }
    },
    startIcon: {
      table: {
        disable: true
      }
    },
    endIcon: {
      table: {
        disable: true
      }
    },
    color: {
      table: {
        disable: true
      }
    },
    variant: {
      table: {
        disable: true
      }
    },
    underline: {
      control: { type: 'select' }
    }
  }}
/>

export const getLink = (args, context) => {
  return context.globals.locale === 'en' ? 'Link' : 'Ссылка';
};

export const getLinkInherit = (args, context) => {
  return context.globals.locale === 'en' ? 'Link with inherited color' : 'Ссылка с унаследованным цветом';
};

export const getLinkColor = (args, context) => {
  return context.globals.locale === 'en' ? 'Link with custom color' : 'Ссылка с произвольным цветом';
};

export const getLinkIcon = (args, context) => {
  return context.globals.locale === 'en' ? 'Link with the icon' : 'Ссылка с иконкой';
};

# Link

<Description of={Link} />

<Canvas>
  <Story name="Demo">
    {(args, context) => (
      <Typography variant="body100" style={{ display: 'flex', flexWrap: 'wrap', gap: '16px' }}>
        <Link {...args} component="a" href="https://google.com" target="_blank">
          {getLink(args, context)}
        </Link>
        <Link {...args} component="a" href="https://google.com" target="_blank" color="inherit">
          {getLinkInherit(args, context)}
        </Link>
        <Link {...args} component="a" href="https://google.com" target="_blank" color="secondary.300">
          {getLinkColor(args, context)}
        </Link>
        <Link
          {...args}
          component="a"
          href="https://google.com"
          target="_blank"
          endIcon={<IconBookmark container containerSize="20px" />}
        >
          {getLinkIcon(args, context)}
        </Link>
      </Typography>
    )}
  </Story>
</Canvas>

<ArgsTable story="Demo" />

Links are aligned within the text.

<Canvas>
  <Story name="Alignment">
    {() => (
      <Typography component="div" variant="body100">
        Lorem ipsum dolor sit amet,{' '}
        <Link component="a" href="#" target="_blank" endIcon={<IconBookmark container containerSize="20px" />}>
          consectetur
        </Link>{' '}
        adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam,
        quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure dolor in{' '}
        <Link component="a" href="#" target="_blank">
          reprehenderit
        </Link>{' '}
        in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat{' '}
        <Link component="button" disabled variant="body100" startIcon={<IconBookmark container containerSize="20px" />}>
          non proident
        </Link>
        , sunt in culpa qui officia deserunt mollit anim id est laborum.
      </Typography>
    )}
  </Story>
</Canvas>

Links have different underline thickness and offset depending on the font variant.

<Canvas>
  <Story name="Typography">
    {(args, context) => {
      const text = getLink(args, context);
      const variants = [
        'h1',
        'h2',
        'h3',
        'h4',
        'h5',
        'h6',
        'subtitle1',
        'subtitle2',
        'body400',
        'body300',
        'body200',
        'body100',
        'mini100',
        'mini200',
        'micro',
        'button',
        'caption',
        'overline'
      ];
      const props = { component: 'a', href: '#', target: '_blank' };
      return (
        <div style={{ display: 'flex', flexWrap: 'wrap', gap: '8px', alignItems: 'baseline' }}>
          {variants.map((variant) => (
            <Link variant={variant} component="a" href="#" target="_blank" {...args}>
              {text} {variant}
            </Link>
          ))}
        </div>
      );
    }}
  </Story>
</Canvas>

## API

<ul>
  <li>
    <LinkTo kind="components-api-link" story="page">
      <code>Link</code>
    </LinkTo>
  </li>
</ul>
