import { Meta, Story, Canvas, ArgsTable, Props } from '@storybook/addon-docs';
import LinkTo from '@storybook/addon-links/react';

import { FileIcon, FileIconBadge, FileIconText, FileIconIcon, FileIconIconVariant } from '.';

<Meta
  title="Components/FileIcon"
  component={FileIcon}
  argTypes={{
    className: {
      table: {
        disable: true
      }
    },
    classes: {
      table: {
        disable: true
      }
    },
    sx: {
      table: {
        disable: true
      }
    },
    textChildren: {
      description: 'Text in text component.',
      name: 'FileIconText.children',
      defaultValue: 'DOCX',
      control: { type: 'text' }
    },
    badgeChildren: {
      description: 'Text in badge component.',
      name: 'FileIconBadge.children',
      defaultValue: 'RAR',
      control: { type: 'text' }
    },
    badgeColor: {
      name: 'FileIconBadge.color',
      defaultValue: '#ff0000',
      control: { type: 'color' },
      description: 'The badge component backgound color.'
    },
    badgeSize: {
      name: 'FileIconBadge.size',
      defaultValue: 'md',
      control: { type: 'select' },
      options: ['md', 'sm'],
      description: 'The badge component size.'
    }
  }}
/>

# FileIcon

`FileIcon` is a component for displaying file extensions.

<Canvas>
  <Story name="Demo">
    {(args, context) => (
      <div
        style={{
          display: 'flex',
          alignItems: 'center',
          gap: '16px'
        }}
      >
        <FileIcon icon={FileIconIconVariant} width={args.width} height={args.height} />
        <FileIcon icon={FileIconIconVariant} width={args.width} height={args.height}>
          <FileIconText>{args.textChildren}</FileIconText>
        </FileIcon>
        <FileIcon width={args.width} height={args.height} />
        <FileIcon width={args.width} height={args.height}>
          <FileIconText>{args.textChildren}</FileIconText>
        </FileIcon>
        <FileIcon icon={FileIconIconVariant} width={args.width} height={args.height}>
          <FileIconBadge children={args.badgeChildren} size={args.badgeSize} color={args.badgeColor} />
        </FileIcon>
        <FileIcon width={args.width} height={args.height}>
          <FileIconBadge children={args.badgeChildren} size={args.badgeSize} color={args.badgeColor} />
        </FileIcon>
      </div>
    )}
  </Story>
</Canvas>

<ArgsTable story="Demo" />

<br />

Children element automatically positioned in visual center depending on component size.

<Canvas>
  <Story
    name="Sizes"
    argTypes={{
      width: {
        table: {
          disable: true
        }
      },
      height: {
        table: {
          disable: true
        }
      },
      children: {
        table: {
          disable: true
        }
      }
    }}
  >
    {(args, context) => (
      <div
        style={{
          display: 'flex',
          alignItems: 'center',
          gap: '16px'
        }}
      >
        <FileIcon>
          <FileIconBadge color="red" size="md" children="ZIP" />
        </FileIcon>
        <FileIcon width={33} height={44}>
          <FileIconBadge color="purple" size="sm" children="RAR" />
        </FileIcon>
        <FileIcon width={30} height={40}>
          <FileIconBadge color="#9672FF" size="sm" children="RAR" />
        </FileIcon>
        <FileIcon width={27} height={36}>
          <FileIconBadge color="#9672FF" size="sm" children="RAR" />
        </FileIcon>
        <FileIcon icon={FileIconIconVariant}>
          <FileIconBadge color="red" size="md" children="ZIP" />
        </FileIcon>
        <FileIcon icon={FileIconIconVariant} width={33} height={44}>
          <FileIconBadge color="purple" size="sm" children="RAR" />
        </FileIcon>
        <FileIcon icon={FileIconIconVariant} width={30} height={40}>
          <FileIconBadge color="#9672FF" size="sm" children="RAR" />
        </FileIcon>
        <FileIcon icon={FileIconIconVariant} width={27} height={36}>
          <FileIconBadge color="#9672FF" size="sm" children="RAR" />
        </FileIcon>
      </div>
    )}
  </Story>
</Canvas>

## API

<ul>
  <li>
    <LinkTo kind="components-api-fileicon" story="page">
      <code>FileIcon</code>
    </LinkTo>
  </li>
  <li>
    <LinkTo kind="components-api-fileiconbadge" story="page">
      <code>FileIconBadge</code>
    </LinkTo>
  </li>
  <li>
    <LinkTo kind="components-api-fileicontext" story="page">
      <code>FileIconText</code>
    </LinkTo>
  </li>
</ul>
