import { Meta } from '@storybook/addon-docs';
import LinkTo from '@storybook/addon-links/react';
import { TableInterface } from '~storybook/components/TableInterface';

<Meta title="Components API/GalleryDescription" />

# GalleryDescription API

```js
import { GalleryDescription } from '@elonkit/react';
```

## Component name

The name `ESGalleryDescription` can be used when providing default props or style overrides in the theme.

## Props

<TableInterface name="GalleryDescriptionProps" variant="props" />

## CSS

<TableInterface name="GalleryDescriptionClasses" variant="css" />

## Demos

<ul>
  <li>
    <LinkTo kind="components-gallery" story="demo">
      <code>Gallery</code>
    </LinkTo>
  </li>
</ul>
