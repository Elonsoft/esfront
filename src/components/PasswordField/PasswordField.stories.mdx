import { Meta, Story, Canvas, ArgsTable, Props, Description } from '@storybook/addon-docs';
import LinkTo from '@storybook/addon-links/react';

import Typography from '@mui/material/Typography';

import { PasswordField } from './PasswordField';

<Meta
  title="Components/PasswordField"
  component={PasswordField}
  parameters={{
    options: { showPanel: true }
  }}
  argTypes={{
    onVisibilityChange: {
      table: {
        disable: true
      }
    },
    labelHidePassword: {
      table: {
        disable: true
      }
    },
    labelShowPassword: {
      table: {
        disable: true
      }
    },
    iconHidePassword: {
      table: {
        disable: true
      }
    },
    iconShowPassword: {
      table: {
        disable: true
      }
    },
    focused: {
      table: {
        disable: true
      }
    },
    hiddenLabel: {
      table: {
        disable: true
      }
    },
    margin: {
      table: {
        disable: true
      }
    },
    ref: {
      table: {
        disable: true
      }
    }
  }}
/>

## PasswordField

<Description of={PasswordField} />

<Canvas>
  <Story name="Demo">{(args) => <PasswordField {...args} />}</Story>
</Canvas>

<ArgsTable story="Demo" />

## API

<ul>
  <li>
    <LinkTo kind="components-api-PasswordField" story="page">
      <code>PasswordField</code>
    </LinkTo>
  </li>
</ul>
