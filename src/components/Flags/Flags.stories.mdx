import { Meta, Story, Canvas } from '@storybook/addon-docs';
import LinkTo from '@storybook/addon-links/react';

import * as Flags from './icons';

<Meta
  title="Components/Flags"
  parameters={{
    options: { showPanel: true }
  }}
/>

# Flags

Collection of flags of countries and their subdivisions. ISO 3166 is used for the naming.

<Canvas>
  <Story name="Demo">
    {(args) => (
      <div style={{ display: 'flex', flexWrap: 'wrap', gap: '16px' }}>
        {Object.keys(Flags).map((flag) => {
          const Component = Flags[flag];
          return <Component key={flag} title={flag.replace('Flag', '')} />;
        })}
      </div>
    )}
  </Story>
</Canvas>

## API

<ul>
  <li>
    <LinkTo kind="components-api-flags" story="page">
      <code>Flags</code>
    </LinkTo>
  </li>
</ul>
