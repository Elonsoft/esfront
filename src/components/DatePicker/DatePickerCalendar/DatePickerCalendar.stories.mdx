import { Meta, Story, Canvas, ArgsTable, Props } from '@storybook/addon-docs';
import LinkTo from '@storybook/addon-links/react';

import { DatePickerCalendar } from '.';

<Meta
  title="Components/DatePicker/DatePickerCalendar"
  component={DatePickerCalendar}
  parameters={{
    options: { showPanel: true }
  }}
  argTypes={{
    classes: {
      table: {
        disable: true
      }
    },
    className: {
      table: {
        disable: true
      }
    },
    sx: {
      table: {
        disable: true
      }
    },
    selection: {
      table: {
        disable: true
      }
    }
  }}
/>

# DatePickerCalendar

This is a DatePickerCalendar.

<Canvas>
  <Story name="Demo">
    {(args, context) => (
      <DatePickerCalendar
        {...args}
        year={+args.year || 2022}
        month={+args.month || 0}
        weekStart={+args.weekStart || 0}
        selection={['2022-05-15T07:53:25.002Z', '2022-06-03T07:53:25.002Z']}
      />
    )}
  </Story>
</Canvas>

<ArgsTable story="Demo" />

## API

<ul>
  <li>
    <LinkTo kind="components-api-DatePickerCalendar" story="page">
      <code>DatePickerCalendar</code>
    </LinkTo>
  </li>
</ul>
