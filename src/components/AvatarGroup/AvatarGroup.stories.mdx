import { Meta, Story, Canvas, ArgsTable, Props, Description } from '@storybook/addon-docs';
import LinkTo from '@storybook/addon-links/react';

import Tooltip from '@mui/material/Tooltip';

import Avatar from '@mui/material/Avatar';
import Button from '@mui/material/Button';
import Box from '@mui/material/Box';

import { AvatarGroup } from '.';

<Meta
  title="Components/AvatarGroup"
  component={AvatarGroup}
  parameters={{
    options: { showPanel: true }
  }}
  argTypes={{
    classes: {
      table: {
        disable: true
      }
    },
    className: {
      table: {
        disable: true
      }
    },
    children: {
      table: {
        disable: true
      }
    },
    sx: {
      table: {
        disable: true
      }
    }
  }}
/>

# AvatarGroup

<Description of={AvatarGroup} />

<Canvas>
  <Story name="Demo">
    {(args, context) => (
      <AvatarGroup size={args.size} direction={args.direction} spacing={args.spacing} cutoutWidth={args.cutoutWidth}>
        <Avatar alt="John Sharp" src="./avatar/1.png" />
        <Avatar alt="John Sharp" src="./avatar/2.png" />
        <Avatar alt="John Sharp" src="./avatar/3.png" />
        <Avatar alt="John Sharp" src="./avatar/4.png" />
        <Avatar alt="John Sharp" src="./avatar/5.png" />
      </AvatarGroup>
    )}
  </Story>
</Canvas>

<ArgsTable story="Demo" />

We can use `AvatarGroup` not only with the `Avatar` component.

<Canvas>
  <Story name="Children">
    {(args, context) => (
      <AvatarGroup size={56} cutoutWidth={5} spacing={-20}>
        <Tooltip placement="top" arrow title="John Sharp">
          <Avatar alt="John Sharp" src="./avatar/1.png" />
        </Tooltip>
        <Tooltip placement="top" arrow title="Sharp John">
          <Avatar alt="Sharp John" src="./avatar/2.png" />
        </Tooltip>
        <Tooltip placement="top" arrow title="John Sharp">
          <Avatar alt="John Sharp" src="./avatar/3.png" />
        </Tooltip>
        <Tooltip placement="top" arrow title="Sharp John">
          <Avatar alt="Sharp John" src="./avatar/4.png" />
        </Tooltip>
        <Tooltip placement="top" arrow title="John Sharp">
          <Avatar alt="John Sharp" src="./avatar/5.png" />
        </Tooltip>
        <Button
          size="56"
          color="tertiary"
          variant="contained"
          sx={{
            borderRadius: '50%'
          }}
        >
          +5
        </Button>
      </AvatarGroup>
    )}
  </Story>
</Canvas>

## API

<ul>
  <li>
    <LinkTo kind="components-api-avatarGroup" story="page">
      <code>AvatarGroup</code>
    </LinkTo>
  </li>
</ul>
