import { Meta, Story, Canvas, ArgsTable, Props, Description } from '@storybook/addon-docs';
import LinkTo from '@storybook/addon-links/react';

import { TabBar } from './TabBar';
import * as stories from './TabBar.stories';

<Meta
  title="Components/TabBar"
  component={TabBar}
  parameters={{
    options: { showPanel: true }
  }}
  argTypes={{
    classes: {
      table: {
        disable: true
      }
    },
    className: {
      table: {
        disable: true
      }
    },
    sx: {
      table: {
        disable: true
      }
    },
    children: {
      table: {
        disable: true
      }
    }
  }}
/>

## TabBar

<Description of={TabBar} />

<Canvas>
  <Story story={stories.Demo} name="Demo" />
</Canvas>

<ArgsTable story="Demo" />

<br />

We can use `label` prop to add labels to `TabBarItem`.

<Canvas>
  <Story story={stories.WithLabels} name="With Labels" />
</Canvas>

## API

<ul>
  <li>
    <LinkTo kind="components-api-TabBar" story="page">
      <code>TabBar</code>
    </LinkTo>
  </li>
  <li>
    <LinkTo kind="components-api-TabBarItem" story="page">
      <code>TabBarItem</code>
    </LinkTo>
  </li>
</ul>
