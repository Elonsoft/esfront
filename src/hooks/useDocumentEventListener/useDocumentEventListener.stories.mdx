import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import LinkTo from '@storybook/addon-links/react';

import * as stories from './useDocumentEventListener.stories';

<Meta
  title="Hooks/useDocumentEventListener"
  parameters={{
    docs: {
      source: {
        type: 'code'
      }
    }
  }}
/>

# useDocumentEventListener

The hook that sets up a function that will be called whenever the specified event is delivered to the `document`.

<Canvas>
  <Story story={stories.Demo} name="Demo" />
</Canvas>

## API

<ul>
  <li>
    <LinkTo kind="hooks-api-useDocumentEventListener" story="page">
      <code>useDocumentEventListener</code>
    </LinkTo>
  </li>
</ul>
