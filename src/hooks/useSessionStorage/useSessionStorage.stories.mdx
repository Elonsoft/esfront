import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import LinkTo from '@storybook/addon-links/react';

import * as stories from './useSessionStorage.stories';

<Meta title="Hooks/useSessionStorage" />

# useSessionStorage

The hook that manages a single `sessionStorage` key.

<Canvas>
  <Story story={stories.Demo} name="Demo" />
</Canvas>

## API

<ul>
  <li>
    <LinkTo kind="hooks-api-useSessionStorage" story="page">
      <code>useSessionStorage</code>
    </LinkTo>
  </li>
</ul>
