import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import LinkTo from '@storybook/addon-links/react';

import * as stories from './useClipboard.stories';

<Meta title="Hooks/useClipboard" />

# useClipboard

The hook that wraps a Clipboard API and exposes read and write methods.

<Canvas>
  <Story story={stories.Demo} name="Demo" />
</Canvas>

## API

<ul>
  <li>
    <LinkTo kind="hooks-api-useClipboard" story="page">
      <code>useClipboard</code>
    </LinkTo>
  </li>
</ul>
