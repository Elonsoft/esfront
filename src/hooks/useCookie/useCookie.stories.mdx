import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import LinkTo from '@storybook/addon-links/react';

import * as stories from './useCookie.stories';

<Meta title="Hooks/useCookie" />

# useCookie

The hook that returns the current value of a cookie, a callback to update the cookie and a callback to remove the
cookie.

<Canvas>
  <Story story={stories.Demo} name="Demo" />
</Canvas>

## API

<ul>
  <li>
    <LinkTo kind="hooks-api-useCookie" story="page">
      <code>useCookie</code>
    </LinkTo>
  </li>
</ul>
