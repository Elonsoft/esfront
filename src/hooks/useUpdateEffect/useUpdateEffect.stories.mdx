import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import LinkTo from '@storybook/addon-links/react';

import * as stories from './useUpdateEffect.stories';

<Meta
  title="Hooks/useUpdateEffect"
  parameters={{
    docs: {
      source: {
        type: 'code'
      }
    }
  }}
/>

# useUpdateEffect

The hook that is simmilar to `useEffect` but ignores the first invocation.

<Canvas>
  <Story story={stories.Demo} name="Demo" />
</Canvas>

## API

<ul>
  <li>
    <LinkTo kind="hooks-api-useUpdateEffect" story="page">
      <code>useUpdateEffect</code>
    </LinkTo>
  </li>
</ul>
