import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import LinkTo from '@storybook/addon-links/react';

import * as stories from './useLocalStorage.stories';

<Meta title="Hooks/useLocalStorage" />

# useLocalStorage

The hook that manages a single `localStorage` key.

<Canvas>
  <Story story={stories.Demo} name="Demo" />
</Canvas>

## API

<ul>
  <li>
    <LinkTo kind="hooks-api-useLocalStorage" story="page">
      <code>useLocalStorage</code>
    </LinkTo>
  </li>
</ul>
