import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import LinkTo from '@storybook/addon-links/react';

import * as stories from './useCallbackDebounce.stories';

<Meta title="Hooks/useCallbackDebounce" />

# useCallbackDebounce

The hook that creates a debounced function.

<Canvas>
  <Story story={stories.Demo} name="Demo" />
</Canvas>

## API

<ul>
  <li>
    <LinkTo kind="hooks-api-useCallbackDebounce" story="page">
      <code>useCallbackDebounce</code>
    </LinkTo>
  </li>
</ul>
