import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import LinkTo from '@storybook/addon-links/react';
import { Description } from '~storybook/components/Description';

import * as stories from './useWindowEventListener.stories';

<Meta
  title="Hooks/useWindowEventListener"
  parameters={{
    docs: {
      source: {
        type: 'code'
      }
    }
  }}
/>

# useWindowEventListener

<Description name="useWindowEventListener" />

<Canvas>
  <Story story={stories.Demo} name="Demo" />
</Canvas>

## API

<ul>
  <li>
    <LinkTo kind="hooks-api-useWindowEventListener" story="page">
      <code>useWindowEventListener</code>
    </LinkTo>
  </li>
</ul>
