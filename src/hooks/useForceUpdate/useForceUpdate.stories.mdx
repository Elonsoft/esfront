import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import LinkTo from '@storybook/addon-links/react';

import * as stories from './useForceUpdate.stories';

<Meta title="Hooks/useForceUpdate" />

# useForceUpdate

The hook that returns a function that forces component to re-render when called.

<Canvas>
  <Story story={stories.Demo} name="Demo" />
</Canvas>

## API

<ul>
  <li>
    <LinkTo kind="hooks-api-useForceUpdate" story="page">
      <code>useForceUpdate</code>
    </LinkTo>
  </li>
</ul>
