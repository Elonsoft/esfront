import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import LinkTo from '@storybook/addon-links/react';

import { FormatSize } from './FormatSize';

<Meta
  title="Components/FormatSize"
  component={FormatSize}
  parameters={{
    options: { showPanel: true }
  }}
  argTypes={{
    children: {
      control: { type: 'text' },
      defaultValue: '1000'
    },
    prefix: {
      control: { type: 'select' }
    },
    metricUnits: {
      table: {
        disable: true
      }
    },
    binaryUnits: {
      table: {
        disable: true
      }
    }
  }}
/>

# FormatSize

This component formats the size based on the metric or binary unit prefix.

<Canvas>
  <Story name="Demo">{(args, context) => <FormatSize {...args} />}</Story>
</Canvas>

<ArgsTable story="Demo" />

## API

<ul>
  <li>
    <LinkTo kind="components-api-formatsize" story="page">
      <code>FormatSize</code>
    </LinkTo>
  </li>
</ul>
