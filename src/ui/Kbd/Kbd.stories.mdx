import { Meta, Story, Canvas, ArgsTable, Props } from '@storybook/addon-docs';
import LinkTo from '@storybook/addon-links/react';

import Typography from '@mui/material/Typography';

import { Kbd } from './Kbd';

<Meta
  title="Components/Kbd"
  component={Kbd}
  parameters={{
    options: { showPanel: true }
  }}
  argTypes={{
    classes: {
      table: {
        disable: true
      }
    },
    className: {
      table: {
        disable: true
      }
    },
    children: {
      control: { type: 'text' },
      defaultValue: 'Ctrl'
    },
    variant: {
      control: { type: 'select' }
    }
  }}
/>

## Kbd

Display keyboard button or keys combination.

<Canvas>
  <Story name="Demo">
    {(args, context) => (
      <Typography component="div" variant="body100">
        Lorem ipsum dolor sit amet <Kbd {...args} /> consectetur adipiscing elit.
      </Typography>
    )}
  </Story>
</Canvas>

<ArgsTable story="Demo" />

## API

<ul>
  <li>
    <LinkTo kind="components-api-kbd" story="page">
      <code>Kbd</code>
    </LinkTo>
  </li>
</ul>
